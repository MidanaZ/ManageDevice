package com.ailab.managedevice.repository;

import com.ailab.managedevice.entity.Borrow_bill;
import org.springframework.data.jpa.repository.JpaRepository;

public interface Borrow_billRepository extends JpaRepository<Borrow_bill, Integer> {
}
