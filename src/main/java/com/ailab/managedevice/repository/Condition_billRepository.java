package com.ailab.managedevice.repository;

import com.ailab.managedevice.entity.Condition_bill;
import org.springframework.data.jpa.repository.JpaRepository;

public interface Condition_billRepository extends JpaRepository<Condition_bill,Integer> {
}
